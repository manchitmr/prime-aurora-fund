{
  "name": "prime-aurora-fund",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild src/editor.js --bundle --format=esm --minify --outfile=dashboard/editor.js && esbuild src/brand.js --bundle --format=iife --global-name=PABrand --minify --outfile=dashboard/brand.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": "true",
  "dependencies": {
    "@netlify/database": "^1.1.0",
    "@netlify/functions": "^5.3.0",
    "@netlify/identity": "^1.2.0",
    "drizzle-orm": "^1.0.0-beta.22",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "drizzle-kit": "^1.0.0-beta.22",
    "esbuild": "^0.28.1"
  }
}
